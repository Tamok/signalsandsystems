# Archived Scripts

One-off migration scripts preserved for historical reference. Not part of the active build or dev workflow.

- `batch-rename-resources.ps1` — PowerShell helper used during the TFP `_resources/` rename migration.
- `standardize-resources.ts` — Normalized per-article resource folder conventions across collections.

If you need to re-run one of these, review it against the current repo state first — paths and collection names may have shifted.
